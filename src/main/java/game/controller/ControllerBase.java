package game.controller;

/**
 * Right now this serves only one purpose: to use it in @SpringBootApplication's scanBasePackageClasses property, to let Spring know what package to scan.
 * 
 * @author Gergely_Gal
 *
 */
public interface ControllerBase {

}